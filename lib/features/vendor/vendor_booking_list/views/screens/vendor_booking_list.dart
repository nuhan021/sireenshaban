import 'package:flutter/material.dart';

class VendorBookingList extends StatelessWidget {
  const VendorBookingList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
    );
  }
}
